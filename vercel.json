{
  "regions": ["sfo1"],
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install --no-audit --no-fund",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
